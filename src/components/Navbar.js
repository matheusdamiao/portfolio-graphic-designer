import styled from "styled-components";

const NavBar = styled.div`
    height: 80px;
    background-color: rgba(81, 103, 31, 0.1);
    top: 0;
    position: sticky;
    display: flex;
    z-index: 999;
    align-items: center;
    justify-content: space-between;
    padding: 0em 1em;
`

export default NavBar